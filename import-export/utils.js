/* Export Nombrado o Named Export */

export const sumar = (a, b) => a + b
export const restar = (a, b) => a - b

export const PI = 3.1416

/* Export por defecto o Export Default */

const dividir = (a, b) => a / b

export default dividir
